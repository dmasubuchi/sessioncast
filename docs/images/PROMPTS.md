# Illustrations

These images are generated with Nanobanana (Gemini 3 image generation).
Each source file in this directory has a corresponding prompt comment in the markdown that references it.

Run: `python ~/.claude/skills/nanobanana/scripts/generate.py <prompt> -o docs/images/<file>.png`
